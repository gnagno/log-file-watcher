h1= title
p watching 
  em #{watched}
form#filters.filters
  input(type='checkbox', name="filter-regex", id="filter-regex")
  label(for:"filter-regex") regex
  span.error
  br
  input(type="text", name="filter", id="filter")
.log-overflow
  table.log.file-watcher
  .noline no line to display
script(type:"text/javascript") 
  $(function() {
     watcher.setupSingleLog("#{watched}");
  })
